{
  "name": "assignment-3-docker-github-actions",
  "version": "1.0.0",
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test",
    "lint": "bunx oxlint src/"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "devDependencies": {
    "oxlint": "^0.16.6"
  }
}
